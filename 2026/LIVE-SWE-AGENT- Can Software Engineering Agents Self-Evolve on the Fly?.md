该论文提出 LIVE-SWE-AGENT，首个能在运行时（on-the-fly）自主、持续自我演化的软件工程智能体。其核心思想是将智能体自身视为可被修改的软件系统，在解决真实世界软件问题的过程中，动态地扩展和改进其能力，而无需任何离线训练或预设的复杂工具集。

LIVE-SWE-AGENT 从一个极简的初始智能体出发（例如仅具备 bash 命令访问权限的 mini-SWE-agent），在常规的问题解决循环中，智能体不仅能执行常规操作（如运行测试、编辑文件），还能主动合成、修改并执行自定义工具。这些工具以可执行脚本（如 Python 脚本）的形式存在，可以是通用的（如增强版的文件编辑器、代码搜索器），也可以是针对当前问题高度定制的（如解析特定二进制格式的分析器）。为了引导这一自我演化过程，系统在每次环境反馈后，通过一个轻量级的反思提示（reflection prompt）显式询问智能体：“是否可以创建或修改一个工具来加速当前任务的进展？” 这一机制将工具创建提升为与普通动作同等重要的决策点。

该方法有效解决了现有研究的两大局限。首先，通过动态创建任务相关的工具，智能体的动作空间能够自适应地贴合当前问题的需求，避免了固定工具集的僵化和不足。其次，通过将能力提升从昂贵的离线训练转移到在线演化，LIVE-SWE-AGENT 消除了对特定基准或大语言模型（LLM）的依赖，具有极强的通用性和成本效益。工具的创建并非一次性预处理，而是一个与问题解决交织的迭代过程：随着智能体对故障模式理解的深入，它可以不断优化或发明新的工具。

作者在 SWE-bench Verified 和更具挑战性的 SWE-Bench Pro 基准上对 LIVE-SWE-AGENT 进行了评估。结果显示，在不使用任何测试时缩放（test-time scaling）的情况下，LIVE-SWE-AGENT 分别取得了 77.4% 和 45.8% 的解决率，超越了所有现有的开源和商业智能体系统，创造了新的技术纪录。消融实验证实，工具创建能力（尤其是结合反思提示）对性能提升至关重要。工具分析表明，智能体能生成多样化的工具，涵盖通用功能和问题特定的定制化功能，且在多语言、多仓库的复杂环境中同样有效。

与需要耗费数万美元进行离线训练的自进化智能体（如 DGM）相比，LIVE-SWE-AGENT 无需任何离线成本，其演化完全在运行时即时发生。这不仅大幅降低了开销，还使其能灵活适配不同的底层 LLM。实验表明，更强的 LLM 能从该框架中获益更多，展现出良好的未来扩展性。LIVE-SWE-AGENT 为构建真正自主、可进化的软件工程智能体提供了一条高效、通用且极具前景的新路径。项目已开源，并被用于构建统一的 LLM 软件能力评估排行榜。